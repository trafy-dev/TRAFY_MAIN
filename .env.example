# Supabase project (Settings → API). Both are safe to expose to the browser —
# the anon key only allows what Row Level Security policies permit, which for
# newsletter_subscribers is INSERT-only (see docs/supabase/newsletter_subscribers.sql).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
